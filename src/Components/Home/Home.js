import React from 'react';
import animateSvg from '../../alone-animate.svg';
const Home = () => {
    return (
        <section className={'home-container'} >
            <img src={animateSvg} className={'img-svg'} alt={'not available'} />
        </section>
    )
}

export default Home;